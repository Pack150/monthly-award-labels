# monthly-award-labels
Takes the CSV output of a Scoutbook.com Purchase Order and creates labels for award envelopes.

Reads from standard input. Run like this:

```
$ ./makeLabels.pl < PO45872.csv
```
